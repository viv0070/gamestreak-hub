// Future feature: alert when "Buy Now" is clicked
document.querySelectorAll("button").forEach(button => {
    button.addEventListener("click", () => {
        alert("Redirecting to payment gateway... (feature under development)");
    });
});
